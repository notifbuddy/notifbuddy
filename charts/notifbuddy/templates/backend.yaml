{{- $fullname := include "notifbuddy.fullname" . }}
apiVersion: v1
kind: Service
metadata:
  name: {{ $fullname }}-backend
  labels:
    {{- include "notifbuddy.labels" . | nindent 4 }}
spec:
  ports:
    - port: 8080
      targetPort: http
      name: http
  selector:
    {{- include "notifbuddy.selectorLabels" (dict "ctx" . "component" "backend") | nindent 4 }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ $fullname }}-backend
  labels:
    {{- include "notifbuddy.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.backend.replicas }}
  selector:
    matchLabels:
      {{- include "notifbuddy.selectorLabels" (dict "ctx" . "component" "backend") | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "notifbuddy.podLabels" (dict "ctx" . "component" "backend") | nindent 8 }}
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/configmap-backend.yaml") . | sha256sum }}
        {{- with .Values.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      {{- with .Values.image.pullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      securityContext:
        {{- include "notifbuddy.podSecurityContext" . | nindent 8 }}
      {{- with include "notifbuddy.waitForPostgres" . }}
      initContainers:
        {{- . | nindent 8 }}
      {{- end }}
      containers:
        - name: backend
          image: {{ include "notifbuddy.image" (dict "ctx" . "name" "backend") | quote }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          securityContext:
            {{- include "notifbuddy.containerSecurityContext" . | nindent 12 }}
          # The whole Secret: the config references secrets as ${VAR}, and
          # which ones exist depends on the integrations that are enabled.
          # Anything the config does not reference is simply unused.
          envFrom:
            - secretRef:
                name: {{ include "notifbuddy.secretName" . }}
          env:
            - name: CONFIG_FILE
              value: /config/config.yaml
            - name: NB_ENV
              value: prod
          ports:
            - name: http
              containerPort: 8080
          # /healthz sits outside the auth and CORS middleware, so it answers
          # without a session — every other route, /ping included, would
          # report a healthy server as 401.
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 15
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
          resources:
            {{- toYaml .Values.backend.resources | nindent 12 }}
          volumeMounts:
            - name: config
              mountPath: /config
              readOnly: true
      volumes:
        - name: config
          configMap:
            name: {{ $fullname }}-backend
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
