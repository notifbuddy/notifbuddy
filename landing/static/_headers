# Cloudflare Workers static-assets headers file.
# Hashed build output never changes at a given URL — cache it hard. Everything
# else (HTML, root assets) keeps the default revalidate behavior.
/_app/immutable/*
  Cache-Control: public, max-age=31536000, immutable
